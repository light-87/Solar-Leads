VITE_ADMIN_PIN=24012002

# Database master switch (controls whether the site comes up after login).
#   false / unset -> site is OFFLINE: shows "database lost connection" message
#   true          -> site is ONLINE:  the vendor directory loads
# After changing this, rebuild & redeploy for it to take effect.
VITE_DATABASE_ONLINE=false
